/**
 * @deprecated Import from ../_shared/exit-shim instead.
 * This re-export exists for backward compatibility during the consolidation migration.
 */
export { SHIM_PATH, SHIM_SCRIPT, ensureExitShim } from "../_shared/exit-shim";
