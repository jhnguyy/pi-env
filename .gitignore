node_modules/
# Extension build artifacts — regenerated by `bun run build` / postinstall
.pi/extensions/*/dist/
auth.json
settings.json
models.json
*.env
sessions/
.DS_Store

# Machine-local config and data — never committed
.pi/handoffs/
permissions.json
permissions-audit.jsonl
node_modules
.pi/extensions/dead-code-report.json
