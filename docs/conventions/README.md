# Code conventions

This directory holds repo-specific implementation conventions. Navigate the directory by filename and read the convention that matches the changed area. Do not treat the directory as a required checklist. Do not add a linked file catalog here because the directory structure is the catalog.
